import { AppMarkDirective } from './mark.directive';

describe('AppMarkDirective', () => {
  it('should create an instance', () => {
    const directive = new AppMarkDirective();
    expect(directive).toBeTruthy();
  });
});
